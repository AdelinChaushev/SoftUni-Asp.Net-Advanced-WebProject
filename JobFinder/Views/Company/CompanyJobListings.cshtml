@model IEnumerable<JobListingOutputViewModel>
@{
    ViewData["Title"] = "Here are the company job listings";
}
<h1>@ViewData["Title"]</h1>
<div class="row">
     @if(Model.Any())
     {
         foreach(var jobListing in Model)
         {
              <partial name="_SearchJobListingPartial" model="jobListing"></partial>    
         }
     }
    @if(!Model.Any())
    {
        <h2>No job listings yet.</h2>
    }
     
</div>